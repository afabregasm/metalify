<h2>{{artist.0.name}} Search results:</h2>
<div>
    {{#each artist}}
    <div onclick="location.href='artist/{{id}}'">
        <h3>{{name}}</h3>
        <p><img src="{{images.0.url}}" alt="{{name}}" width="20%" height="auto" /></p>
    </div>
    {{/each}}
</div>
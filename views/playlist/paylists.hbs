<h2>{{album.0.artists.0.name}} Albums:</h2>

    {{#each album}}
    <div>
        <h3>{{name}}</h3>
        <p><img src="{{images.0.url}}" alt="{{name}}" width="20%" height="auto" /></p>
       
        <a href="/tracks/{{id}}">View tracks</a>
    </div>
    {{/each}}
